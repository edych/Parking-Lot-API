version: 2.1
orbs:
  codecov: codecov/codecov@3.2.2
jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run:
          name: Build
          command: mvn -B clean install
workflows:
  sample:
    jobs:
      - build-and-test
      - codecov/upload
